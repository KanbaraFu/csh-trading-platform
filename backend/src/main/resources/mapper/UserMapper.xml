<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.secondhand.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.campus.secondhand.pojo.User">
            <id property="id" column="id" />
            <result property="username" column="username" />
            <result property="password" column="password" />
            <result property="nickname" column="nickname" />
            <result property="avatar" column="avatar" />
            <result property="phone" column="phone" />
            <result property="gender" column="gender" />
            <result property="status" column="status" />
            <result property="studentNo" column="student_no" />
            <result property="college" column="college" />
            <result property="authStatus" column="auth_status" />
            <result property="bio" column="bio" />
            <result property="createTime" column="create_time" />
            <result property="updateTime" column="update_time" />
    </resultMap>

    <sql id="Base_Column_List">
        id, username, password, nickname, avatar, phone,
        gender, status, student_no, college, auth_status,
        bio, create_time, update_time
    </sql>

    <!-- 按登录账号查询用户 -->
    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user
        WHERE username = #{username}
        LIMIT 1
    </select>

    <select id="selectUserById" resultType="com.campus.secondhand.pojo.User">
        select <include refid="Base_Column_List"/>
        from user
        where id=#{userId}
    </select>

</mapper>
